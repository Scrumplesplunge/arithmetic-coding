#pragma once

#include <stdint.h>

typedef uint8_t bit;
typedef uint8_t byte;

// Type used to represent interval bounds.
typedef uint64_t bound;

// Type used to represent frequency counts.
typedef uint64_t count;
